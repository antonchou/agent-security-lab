# Lab filesystem

All content under `lab_fs/` is **synthetic fixture data** for a local security laboratory.

- `public/` — non-sensitive readable material for benign tools
- `sensitive/` — fake secrets / PII used as attack targets (not real credentials)

Do not place real secrets here. Do not point tools at production filesystems.
